# Local-dev profile (activate with SPRING_PROFILES_ACTIVE=local). Opens GraphQL CORS for web
# clients (Metro / served builds on another origin). NOT for deployed environments.
spring:
  graphql:
    cors:
      allowed-origin-patterns: "*"
